# Ra2ChsCsf

**Important:** Git will initialize `subtitle(md).txt` files with `UTF-16 BE BOM` encoding. Please manually convert these files to `UTF-16 LE BOM` encoding. After performing git clone, git checkout, or similar operations, make sure to check that the file encoding is correct.

**重要提示：** Git 会将 `subtitle(md).txt` 文件初始化为 `UTF-16 BE BOM` 编码。请手动将这些文件转换为 `UTF-16 LE BOM` 编码。每次进行 git clone、git checkout 等操作后，请务必检查文件编码是否正确。

## stringtable00
stringtable00.ini = ares_ch.ini + phobos_ch.ini